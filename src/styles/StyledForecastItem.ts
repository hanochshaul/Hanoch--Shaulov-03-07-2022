import { Paper, styled } from "@mui/material";

const StyledForecastItemContainer = styled(Paper)(({ theme }) => ({
    textAlign: 'center',
    padding: 10,
    height: 280
}));

export default StyledForecastItemContainer;